/*
import Sequelize from 'sequelize'
import path from 'path'

const sequelize = new Sequelize({
    dialect: 'sqlite',
    storage: path.join(_dirname, '..', '..', 'public', 'database', 'database.sqlite' )
})

export default sequelize
*/
